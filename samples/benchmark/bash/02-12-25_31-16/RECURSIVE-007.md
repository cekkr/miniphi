# Bash Recursive Prompt Report

- Generated at: 2025-12-02T15:31:41.932Z
- Source root: `samples\bash`
- Directory depth: 1
- Focus files (5):
- eval.c
- execute_cmd.c
- jobs.c
- shell.c
- telemetry.c

## Directory Tree (depth ≤ 1)
```
eval.c
execute_cmd.c
jobs.c
README.md
shell.c
telemetry.c
```

## Prompt Statistics
| Stage | Prompt chars | Response chars | Duration | Notes |
| --- | --- | --- | --- | --- |
| offline-overview | 57 | 1009 | 0.0s | offline-mode |

## Stage Transcripts
### Stage: offline-overview

**Prompt preview**
```
eval.c
execute_cmd.c
jobs.c
README.md
shell.c
telemetry.c
```

**Response**

## Offline Benchmark Summary
LM Studio prompts were bypassed in favor of a deterministic walkthrough.
### Directory Sketch
```
eval.c
execute_cmd.c
jobs.c
README.md
shell.c
telemetry.c
```
### File Highlights
- eval.c: #include <stdio.h> #include <string.h> void execute_command_internal(const char *command); static int read_command(char *buffer, size_t size); static int valida
- execute_cmd.c: #include <stdio.h> #include <string.h> struct execution_plan { char command[64]; int steps; }; static void prepare_environment(struct execution_plan *plan); sta
- jobs.c: #include <stdio.h> void sync_jobs(void) { printf("[jobs] syncing job table\\n"); } void prune_jobs(void) { printf("[jobs] pruning completed jobs\\n"); }
- shell.c: #include <stdio.h> int reader_loop(void); void execute_command_internal(const char *command); void sync_jobs(void); void prune_jobs(void); void flush_telemetry(
- telemetry.c: #include <stdio.h> void flush_telemetry(void) { printf("[telemetry] flushing buffered metrics\\n"); }


## Final Synthesis
## Offline Benchmark Summary
LM Studio prompts were bypassed in favor of a deterministic walkthrough.
### Directory Sketch
```
eval.c
execute_cmd.c
jobs.c
README.md
shell.c
telemetry.c
```
### File Highlights
- eval.c: #include <stdio.h> #include <string.h> void execute_command_internal(const char *command); static int read_command(char *buffer, size_t size); static int valida
- execute_cmd.c: #include <stdio.h> #include <string.h> struct execution_plan { char command[64]; int steps; }; static void prepare_environment(struct execution_plan *plan); sta
- jobs.c: #include <stdio.h> void sync_jobs(void) { printf("[jobs] syncing job table\\n"); } void prune_jobs(void) { printf("[jobs] pruning completed jobs\\n"); }
- shell.c: #include <stdio.h> int reader_loop(void); void execute_command_internal(const char *command); void sync_jobs(void); void prune_jobs(void); void flush_telemetry(
- telemetry.c: #include <stdio.h> void flush_telemetry(void) { printf("[telemetry] flushing buffered metrics\\n"); }

---
Report generated by benchmark/scripts/bash-recursive-prompts.js.