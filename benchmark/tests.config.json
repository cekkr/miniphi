{
  "tests": [
    {
      "name": "benchmark-cli-coverage",
      "description": "Run the CLI benchmark unit test suite (merged with unit-tests-js).",
      "command": "node --test unit-tests-js/cli-benchmark.test.js",
      "cwd": ".",
      "timeoutMs": 300000,
      "logDir": "cli-benchmark"
    }
  ]
}
