{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MiniPhi Agent Action Turn",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "task",
    "summary",
    "summary_updates",
    "actions",
    "needs_more_context",
    "missing_snippets"
  ],
  "properties": {
    "task": {
      "type": "string",
      "minLength": 1,
      "description": "Repeat the operator task in <= 12 words."
    },
    "summary": {
      "type": "string",
      "description": "Natural-language status of what you are doing this turn."
    },
    "summary_updates": {
      "type": "array",
      "description": "Short chronological progress updates (can be empty).",
      "items": { "type": "string" },
      "default": []
    },
    "actions": {
      "type": "array",
      "description": "Ordered actions to perform this turn. Emit a single `finish` action (no others) when the task is complete.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "reason"],
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "read_file",
              "list_dir",
              "search_text",
              "web_research",
              "write_file",
              "edit_file",
              "run_cmd",
              "finish"
            ],
            "description": "The action kind. read_file/list_dir/search_text/web_research are auto-run; write_file/edit_file/run_cmd require operator approval; finish ends the session."
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "Why this action is needed (shown to the operator)."
          },
          "path": {
            "type": "string",
            "description": "Repo-relative POSIX path for read_file/list_dir/write_file/edit_file (never absolute, no `..`)."
          },
          "content": {
            "type": "string",
            "description": "Full file content for write_file, or the whole-file replacement for edit_file when no anchor is given."
          },
          "anchor": {
            "type": "string",
            "description": "For edit_file: the complete exact existing substring to replace, not the start of a range. Only these literal characters are removed, so include the whole old function/block when replacing a function/block. Must occur exactly once."
          },
          "replacement": {
            "type": "string",
            "description": "For edit_file: the text inserted in place of the complete literal `anchor`. Do not repeat surrounding source that is outside the anchor."
          },
          "term": {
            "type": "string",
            "description": "Search term for search_text."
          },
          "query": {
            "type": "string",
            "description": "Search query for web_research. Use it before choosing unfamiliar or time-sensitive libraries."
          },
          "max_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Maximum web results to return for web_research (default 5)."
          },
          "command": {
            "type": "string",
            "description": "Shell command for run_cmd."
          },
          "danger": {
            "type": "string",
            "enum": ["low", "mid", "high"],
            "description": "Operator-facing risk hint for write_file/edit_file/run_cmd."
          },
          "expected_hash": {
            "type": "string",
            "description": "Optional sha256 of the file content you last read, guarding edit_file against concurrent changes."
          }
        }
      }
    },
    "needs_more_context": {
      "type": "boolean",
      "description": "True when you cannot proceed without more of the repo.",
      "default": false
    },
    "missing_snippets": {
      "type": "array",
      "description": "Minimal repo-relative files/snippets required before proceeding (e.g. src/index.js).",
      "items": { "type": "string" },
      "default": []
    },
    "context_sufficient": {
      "type": "boolean",
      "description": "False when the loaded context graph is not precise enough for the current step; MiniPhi reforms the graph and re-prompts once. Omit or set true when the context is adequate.",
      "default": true
    },
    "context_gap": {
      "type": "string",
      "description": "Concrete description of what is missing or imprecise (node ids, symbols, file paths) when context_sufficient is false."
    },
    "context_ops": {
      "type": "array",
      "description": "Context graph language: operations applied to your own layered context after this turn (max 12). See the context guide in the system prompt.",
      "default": [],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["op"],
        "properties": {
          "op": {
            "type": "string",
            "enum": [
              "pin",
              "unpin",
              "boost",
              "expand",
              "collapse",
              "drop",
              "note",
              "link",
              "open_subtask",
              "close_subtask",
              "focus"
            ],
            "description": "expand loads a stub/digest back in full; pin keeps a node in every prompt; boost raises its selection score; collapse replaces it with your digest; drop discards it; note records a durable fact; link relates two nodes; open_subtask/close_subtask scope a sub-conversation; focus switches to an existing subtask."
          },
          "node": {
            "type": "string",
            "description": "Target node id as shown in [brackets] (required by pin/unpin/boost/expand/collapse/drop/focus)."
          },
          "label": {
            "type": "string",
            "description": "Short label for note/open_subtask."
          },
          "text": {
            "type": "string",
            "description": "Body for note, digest text for collapse, goal for open_subtask, outcome for close_subtask."
          },
          "layer": {
            "type": "string",
            "enum": ["plan", "subtask", "evidence", "scratch"],
            "description": "Layer for note (mission/contract are runtime-owned)."
          },
          "importance": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Selection importance for note/boost (0..1)."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "description": "For expand: start the loaded window at this character of the node, to page past content you have already seen."
          },
          "from": {
            "type": "string",
            "description": "Source node id for link."
          },
          "to": {
            "type": "string",
            "description": "Target node id for link."
          },
          "relation": {
            "type": "string",
            "enum": [
              "derived_from",
              "supports",
              "refines",
              "supersedes",
              "answers",
              "blocks",
              "child_of"
            ],
            "description": "Edge relation for link."
          },
          "reason": {
            "type": "string",
            "description": "Why this reshaping is needed (recorded in telemetry)."
          }
        }
      }
    }
  }
}
