{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MiniPhi Recompose Code Generation",
  "type": "object",
  "additionalProperties": false,
  "required": ["code", "needs_more_context", "missing_snippets"],
  "properties": {
    "code": {
      "type": ["string", "null"],
      "description": "Complete source code for the requested file without markdown fences."
    },
    "language": {
      "type": ["string", "null"],
      "description": "Optional language identifier for the generated source."
    },
    "warnings": {
      "type": "array",
      "description": "Caveats or uncertainty to keep in mind during recomposition.",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "needs_more_context": {
      "type": "boolean",
      "description": "Set to true when the plan/narrative is insufficient to generate code.",
      "default": false
    },
    "missing_snippets": {
      "type": "array",
      "description": "Minimal snippets or files needed before retrying.",
      "items": {
        "type": "string"
      },
      "default": []
    }
  }
}
